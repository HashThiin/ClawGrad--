# ClawGrad — 任务分工文档

> **目标**：三周内给现有批改系统加上用户体系，包装成可演示的完整产品
> **范围**：用户认证 → 学生端（登录/提交/结果/历史记录）
> **时间**：3 周，以周为单位
> **团队**：技术负责人+ A（测试及其它）+ B（完善框架）+ C（前端页面 + UI）

---

## 📊 现有代码审计报告

### ✅ 已完成（可直接使用）

| 模块 | 文件 | 状态 |
|------|------|------|
| **四阶段流水线** | `pipeline/` 6 文件 | ✅ 完整，核心资产，不动 |
| **OpenClaw 客户端** | `OpenClawClientService.java` | ✅ 完整，多模态 |
| **批改控制器** | `AssignmentGradingController.java` | ✅ 完整 |
| **批改服务** | `AssignmentGradingService.java` | ✅ 完整 |
| **模型目录** | `ModelCatalogService.java` | ✅ 完整，10 个模型 |
| **文件存储** | `FileStorageService.java` | ✅ 完整 |
| **前端批改页** | `GradePage.tsx` + `ResultPage.tsx` | ✅ 完整可用 |
| **前端 API** | `services/api.ts` | ✅ 完整 |
| **Docker 编排** | `docker-compose.yml` | ✅ 基本可用 |

### ⚠️ 已知问题（本次必须修复）

| # | 问题 | 位置 | 说明 | 修复人 |
|---|------|------|------|--------|
| 1 | Redis 端口不一致 | `application.yml` 写 6379→6380 不匹配 | 后端连不上 Redis | B |
| 2 | GradingTaskStore 用内存 Map | `service/GradingTaskStore.java` | 容器重启后任务全部丢失 | B |
| 3 | @EnableAsync 无线程池配置 | `OpenClawGradingApplication.java` | 默认线程池不可控 | B |
| 4 | 无认证 | 全局 | 任何人都可以调用接口 | 组长 |
| 5 | 无用户/提交历史 | 全局 | 没有用户概念，没有历史记录 | 组长+B |

### 🎯 本次新增内容

| 模块 | 说明 | 负责人 |
|------|------|--------|
| 用户认证系统 | 注册/登录/JWT/Spring Security | 组长 |
| 用户 + 提交历史持久化 | MySQL 存用户 + 提交记录 | B |
| Redis 持久化任务状态 | 替换内存 Map | B |
| 学生端前端 | 登录/注册/仪表盘/历史记录 | C |
| 全面测试 | 单元测试 + 集成测试 + API 文档 | A |

---

## 👥 四人分工

### 🏗️ 技术负责人

> 定位：认证核心 + 权限集成 + 全局统筹

**为什么这些必须你做**：认证是系统的"门锁"，涉及安全，需要最了解架构的人来设计。权限集成需要知道现有 Controller 和流水线怎么改。

#### 具体任务

**Week 1：认证模块（核心）**

| # | 任务 | 交付文件 | 说明 |
|---|------|---------|------|
| 1 | Spring Security 配置 | `config/SecurityConfig.java` | 放行 `/auth/**` + 其他接口需 JWT + CORS |
| 2 | JWT 工具类 | `auth/util/JwtUtil.java` | 生成/解析/验证，从 `.env` 读密钥 |
| 3 | JWT 过滤器 | `auth/filter/JwtAuthenticationFilter.java` | 拦截请求，注入 SecurityContext |
| 4 | 用户 Entity + Repository | `auth/entity/User.java`<br>`auth/repository/UserRepository.java` | JPA 实体（id, username, password, nickname, role, timestamps） |
| 5 | 认证 DTO | `auth/dto/` 下 3 个文件 | LoginRequest, RegisterRequest, AuthResponse |
| 6 | 认证 Service | `auth/service/AuthService.java` | 注册 + 登录 + BCrypt 密码加密 |
| 7 | 认证 Controller | `auth/controller/AuthController.java` | `POST /auth/register` + `POST /auth/login` |
| 8 | 全局异常处理 | `common/exception/GlobalExceptionHandler.java`<br>`common/dto/ApiResponse.java` | 统一 `{ code, message, data }` 响应格式 |

**Week 2：权限集成**

| # | 任务 | 交付文件 | 说明 |
|---|------|---------|------|
| 9 | 批改接口加权限 | `controller/AssignmentGradingController.java` | 所有接口加 `@PreAuthorize("isAuthenticated()")` |
| 10 | 批改查询加权限 | `controller/GradingController.java` | 同上加认证，绑定 userId |
| 11 | GradingTaskStore 加 userId | `service/GradingTaskStore.java` | 任务归属用户，防止越权查询 |
| 12 | `.env.example` 更新 | `.env.example` | 补充 `JWT_SECRET` 等配置 |
| 13 | 代码审查 + 合并 | — | 审核 B/C 的 PR |

**Week 3：联调 + 最终审核**

| # | 任务 | 说明 |
|---|------|------|
| 14 | 全链路联调 | 注册→登录→提交批改→查看结果 |
| 15 | 解决组员技术问题 | 随时支援 |
| 16 | 最终代码审核 | 合并前审查，确保质量 |
| 17 | 演示准备 | 准备最终演示流程 |

---

### 🔧 组员 B — 完善框架

> 定位：数据库 + Redis 修复 + 提交持久化 + 后端基础设施

**你的角色说明**：B 负责让系统"真正能跑"——修复已知的 Redis 问题，把内存任务存储改成持久化，加上提交历史功能。这些是系统稳定运行的基础。

#### 具体任务

**Week 1：修复 + 数据库 + 用户 Entity**

| # | 任务 | 交付文件 | 说明 |
|---|------|---------|------|
| 1 | 修复 Redis 端口 | `application.yml` | 确保 Redis 端口与 docker-compose 一致（6379） |
| 2 | 建表 SQL 脚本 | `resources/db/migration/V1__init_schema.sql` | 用户表 + 提交记录表 |
| 3 | 用户 Entity + Repository | `auth/entity/User.java`<br>`auth/repository/UserRepository.java` | 配合组长（组长定义接口，B 实现 JPA 部分） |
| 4 | 异步线程池配置 | `config/AsyncConfig.java` | 替换默认线程池为可控配置 |
| 5 | 提交记录 Entity | `submission/entity/Submission.java` | 字段：id, userId, taskId, question, modelId, score, status, timestamps |
| 6 | 提交记录 Repository | `submission/repository/SubmissionRepository.java` | `findByUserId` + `findByTaskId` |

**需要创建的数据库表：**

```sql
-- 用户表
CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    role ENUM('STUDENT') DEFAULT 'STUDENT',
    nickname VARCHAR(50),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_username (username)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 提交记录表
CREATE TABLE submissions (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    task_id VARCHAR(64) UNIQUE NOT NULL,
    question TEXT,
    model_id VARCHAR(100),
    status ENUM('PROCESSING','COMPLETED','FAILED','TIMEOUT') DEFAULT 'PROCESSING',
    total_score DOUBLE,
    max_score DOUBLE,
    result_json JSON COMMENT '完整批改结果',
    submitted_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    completed_at DATETIME,
    INDEX idx_user_time (user_id, submitted_at DESC),
    FOREIGN KEY (user_id) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**Week 2：Redis 持久化 + 提交记录服务**

| # | 任务 | 交付文件 | 说明 |
|---|------|---------|------|
| 7 | GradingTaskStore 改 Redis | `service/GradingTaskStore.java` | `ConcurrentHashMap` → `StringRedisTemplate`，序列化 JSON |
| 8 | 提交记录 Service | `submission/service/SubmissionService.java` | 创建记录 + 查询历史 + 更新状态 |
| 9 | 提交记录 Controller | `submission/controller/SubmissionController.java` | `GET /submissions/my` + `GET /submissions/{taskId}` |
| 10 | 提交回调处理 | `submission/service/SubmissionCallbackHandler.java` | 批改完成后自动写入 submissions 表 |

**B 负责的接口清单：**

```
── 提交记录（学生端）──
GET    /api/v1/submissions/my               — 我的提交历史（按时间倒序）
GET    /api/v1/submissions/{taskId}         — 查看某次提交详情（含批改结果）
```

**Week 3：联调 + 优化**

| # | 任务 | 说明 |
|---|------|------|
| 11 | Redis 序列化验证 | 确认 JSON 无乱码，重启后数据不丢失 |
| 12 | 数据库索引优化 | 确认查询效率 |
| 13 | 接口联调 | 配合 C 的前端，修复接口问题 |
| 14 | Bug 修复 | 根据 A 的测试报告修复 |

---

### 🎨 组员 C — 前端页面 + UI

> 定位：学生端全部页面（除批改页/结果页）+ 认证页 + 路由改造

**你的角色说明**：C 负责把所有前端页面做出来。现有的 GradePage 和 ResultPage 已有且可用，你需要做的是它们"外面"的东西——让用户能登录、能看到历史记录、有完整的导航体验。

#### 具体任务

**Week 1：项目改造 + 认证页面**

| # | 任务 | 交付文件 | 说明 |
|---|------|---------|------|
| 1 | 安装 Zustand | `npm install zustand` | 状态管理库 |
| 2 | 用户状态 Store | `store/authStore.ts` | 管理 token/user/role，提供 login/register/logout 方法 |
| 3 | 改造 api.ts | `services/api.ts` | 请求拦截器带 JWT，401 自动跳登录 |
| 4 | 路由守卫 | `router/ProtectedRoute.tsx` | 未登录重定向到登录页 |
| 5 | 改造 App.tsx | `App.tsx` | 按登录状态渲染不同路由 |
| 6 | 登录页 | `pages/auth/Login.tsx` | 用户名 + 密码 + 登录按钮 |
| 7 | 注册页 | `pages/auth/Register.tsx` | 用户名 + 密码 + 昵称 + 注册 |
| 8 | 改造 HomePage | `pages/HomePage.tsx` | 未登录：介绍页 + 登录/注册按钮；已登录：跳仪表盘 |

**Week 2：学生端页面**

| # | 任务 | 交付文件 | 说明 |
|---|------|---------|------|
| 9 | 学生布局 | `layouts/StudentLayout.tsx` | 顶部导航（Logo + 用户名 + 退出）+ 侧边菜单 |
| 10 | 学生仪表盘 | `pages/student/Dashboard.tsx` | 待处理/已完成统计 + 最近提交 + 快捷入口 |
| 11 | 提交历史页 | `pages/student/History.tsx` | 分页列表：题目摘要 + 分数 + 状态 + 时间 |
| 12 | 提交详情页 | `pages/student/SubmissionDetail.tsx` | 查看某次提交的完整批改结果（复用 ResultPage） |
| 13 | 侧边导航组件 | `components/StudentNav.tsx` | 导航菜单组件 |

**页面流转：**
```
未登录 → HomePage（介绍 + 登录/注册按钮）
  ├─ 登录 → StudentLayout
  │   ├─ Dashboard → 概览 + 快捷提交
  │   ├─ 提交作业 → GradePage（已有）
  │   ├─ 历史记录 → History → 点击 → SubmissionDetail
  │   └─ 退出登录 → HomePage
  └─ 注册 → 自动登录 → StudentLayout
```

**Week 3：UI 打磨**

| # | 任务 | 说明 |
|---|------|------|
| 14 | 响应式适配 | 手机端布局 |
| 15 | 加载状态 | 按钮 loading + 页面骨架屏 |
| 16 | 错误提示 | 统一 Toast |
| 17 | 批改页面微调 | GradePage/ResultPage 的美化（如有需要） |

---

### 🧪 组员 A — 测试及其它

> 定位：全面测试 + 文档 + 部署验证

**你的角色说明**：A 的工作不是最后才开始的——从 Week 1 就开始验证现有代码是否正常，Week 2 测试新模块，Week 3 全面回归。同时负责所有文档，让项目从"能跑"变成"能交付"。

#### 具体任务

**Week 1：测试框架 + 部署验证 + 现有代码验证**

| # | 任务 | 交付文件 | 说明 |
|---|------|---------|------|
| 1 | 搭建测试框架 | `java-backend/src/test/java/...` | `@SpringBootTest` + h2 内存数据库 |
| 2 | Docker 部署验证 | 测试报告 | 独立执行 `docker compose up -d`，记录问题 |
| 3 | 现有接口冒烟测试 | 测试报告 | 用 curl 跑一遍批改全流程 |
| 4 | 测试用例文档 | `docs/TEST-CASES.md` | 列出所有要测的功能点 |
| 5 | 准备测试数据 | `src/test/resources/test-data.sql` | 示例用户 + 示例题目 |

**Week 2：单元测试 + API 文档**

| # | 任务 | 交付文件 | 说明 |
|---|------|---------|------|
| 6 | 认证模块测试 | `auth/service/AuthServiceTest.java` | 注册/登录/密码加密 |
| 7 | 流水线测试 | `pipeline/stage/FeedbackStageTest.java` | JSON 解析容错 |
| 8 | API 测试脚本 | `scripts/api-test.sh` | curl 脚本一键测试 |
| 9 | API 文档 | `docs/API.md` | 整理全部接口为 Markdown |
| 10 | 提交记录测试 | `submission/service/SubmissionServiceTest.java` | 创建/查询/更新 |

**Week 3：集成测试 + 部署文档 + 最终交付**

| # | 任务 | 交付文件 | 说明 |
|---|------|---------|------|
| 11 | 全链路集成测试 | `integration/FullWorkflowTest.java` | 注册→登录→提交→查看结果→历史记录 |
| 12 | 边界/异常测试 | 测试报告 | 大文件、空输入、并发提交 |
| 13 | 部署文档 | `docs/DEPLOY.md` | 从零到部署完成的操作手册 |
| 14 | 用户使用手册 | `docs/USER-GUIDE.md` | 学生怎么用这个系统 |
| 15 | 最终回归测试 | 测试报告 | 合并所有代码后完整跑一遍 |
| 16 | 演示脚本 | `scripts/demo.sh` | 一键演示全流程 |

#### 基础测试用例清单

```
TC-01  注册 — 正常注册，返回成功 + token
TC-02  注册 — 重复用户名，返回错误
TC-03  注册 — 密码太短，返回错误
TC-04  登录 — 正确密码，返回 token
TC-05  登录 — 错误密码，返回 401
TC-06  登录 — 不存在的用户，返回 401
TC-07  带 token 访问批改接口 — 成功
TC-08  不带 token 访问批改接口 — 401
TC-09  过期 token — 401
TC-10  纯文本批改 — 提交 → 获取结果
TC-11  图片批改 — 上传图片 → 获取结果
TC-12  批改进度轮询 — PROCESSING → COMPLETED
TC-13  批改失败 — 返回错误信息
TC-14  批改超时 — 返回超时 + 重试建议
TC-15  提交历史记录 — 查看我的提交列表
TC-16  提交详情 — 查看某次批改结果
TC-17  越权访问 — 查询别人的 taskId → 403
TC-18  超大图片上传（接近 20MB）
TC-19  并发提交多个任务
TC-20  容器重启后 Redis 数据不丢失
TC-21  容器重启后 submission 数据不丢失
TC-22  非法格式请求 — 返回 400
TC-23  缺失必填字段 — 返回 400
TC-24  完整流程 — 注册→登录→提交→查看结果→历史记录
```

---

## 📁 最终目录结构

```
ClawGrad--/
├── java-backend/
│   ├── src/main/java/com/openclaw/grading/
│   │   ├── OpenClawGradingApplication.java     ← B 加 AsyncConfig
│   │   ├── config/
│   │   │   ├── OpenClawGatewayProperties.java  ← 已有
│   │   │   ├── ModelCatalogProperties.java     ← 已有
│   │   │   ├── SecurityConfig.java             ← HT新增
│   │   │   └── AsyncConfig.java                ← B 新增
│   │   ├── common/                             ← HT新增
│   │   │   ├── dto/ApiResponse.java
│   │   │   └── exception/GlobalExceptionHandler.java
│   │   ├── auth/                               ← HT + B
│   │   │   ├── controller/AuthController.java  ← HT
│   │   │   ├── service/AuthService.java        ← HT
│   │   │   ├── repository/UserRepository.java  ← B
│   │   │   ├── entity/User.java                ← B
│   │   │   ├── dto/                            ← HT
│   │   │   ├── filter/JwtAuthenticationFilter.java  ← HT
│   │   │   └── util/JwtUtil.java               ← HT
│   │   ├── submission/                         ← B 新增
│   │   │   ├── controller/SubmissionController.java
│   │   │   ├── service/
│   │   │   │   ├── SubmissionService.java
│   │   │   │   └── SubmissionCallbackHandler.java
│   │   │   ├── repository/SubmissionRepository.java
│   │   │   └── entity/Submission.java
│   │   ├── controller/                         ← 已有（HT修改）
│   │   │   ├── AssignmentGradingController.java  ← 加权限
│   │   │   ├── GradingController.java            ← 加权限
│   │   │   └── ...
│   │   ├── pipeline/                           ← 已有（不动）
│   │   ├── service/
│   │   │   ├── GradingTaskStore.java             ← B 改 Redis
│   │   │   └── ...                              ← 已有
│   │   └── model/dto/                          ← 已有
│   ├── src/main/resources/
│   │   ├── application.yml                     ← B 修复
│   │   └── db/migration/V1__init_schema.sql    ← B 新增
│   └── src/test/java/                          ← A 新增
│
├── frontend/
│   └── src/
│       ├── store/                              ← C 新增
│       │   └── authStore.ts
│       ├── router/                             ← C 新增
│       │   ├── index.tsx
│       │   └── ProtectedRoute.tsx
│       ├── layouts/                            ← C 新增
│       │   └── StudentLayout.tsx
│       ├── components/                         ← C 新增
│       │   └── StudentNav.tsx
│       ├── pages/
│       │   ├── auth/                           ← C 新增
│       │   │   ├── Login.tsx
│       │   │   └── Register.tsx
│       │   ├── student/                        ← C 新增
│       │   │   ├── Dashboard.tsx
│       │   │   ├── History.tsx
│       │   │   └── SubmissionDetail.tsx
│       │   ├── HomePage.tsx                    ← C 改造
│       │   ├── GradePage.tsx                   ← 已有
│       │   └── ResultPage.tsx                  ← 已有
│       └── services/
│           └── api.ts                          ← C 改造
│
├── scripts/                                    ← A 新增
│   ├── api-test.sh
│   └── demo.sh
├── docs/                                       ← A 新增
│   ├── API.md
│   ├── DEPLOY.md
│   ├── USER-GUIDE.md
│   └── TEST-CASES.md
├── docker-compose.yml                          ← B 修复
├── .env.example                                ← 更新
└── TASK-DISTRIBUTION.md                        ← 本文件
```

---

## 📅 三周进度

### Week 1（Day 1-7）：基础搭建

```
Day 1-2  ── HT：Spring Security + JWT + 认证 Controller/Service
         ── B：修复 Redis 端口 + 建表 SQL + 异步配置
         ── A：测试框架 + Docker 部署验证 + 现有接口冒烟测试
         ── C：Zustand + 改造 api.ts + 路由守卫 + 登录/注册页

Day 3-4  ── HT：完成认证模块 + 全局异常处理
         ── B：创建 User/Submission Entity + Repository
         ── A：写测试用例文档 + 准备测试数据
         ── C：完成登录注册全链路 + 改造 HomePage

Day 5-7  ── HT：合并 Week 1 成果
         ── B：Entity/Repository 联调
         ── A：跑认证模块测试
         ── C：登录注册 UI 优化

📌 里程碑：能注册、登录、拿到 token，前端能跳转
```

### Week 2（Day 8-14）：核心功能

```
Day 8-10  ── HT：现有 Controller 加权限 + TaskStore 加 userId
          ── B：GradingTaskStore 改 Redis + SubmissionService
          ── A：流水线单元测试 + API 测试脚本
          ── C：StudentLayout + Dashboard + 历史记录页
T
Day 11-12 ── HT：全链路联调
          ── B：SubmissionController + 回调处理
          ── A：批改接口测试 + API 文档
          ── C：提交详情页 + 导航组件

Day 13-14 ── HT：联调修复
          ── B：接口联调
          ── A：跑完所有单元测试
          ── C：页面联调 + 修复
HT
📌 里程碑：注册→登录→提交批改→查看结果→历史记录，全流程跑通
```

### Week 3（Day 15-21）：测试打磨

```
Day 15-17 ── HT：代码审查 + 合并
          ── B：Redis 验证 + Bug 修复
          ── A：集成测试（24 用例）+ 边界测试
          ── C：响应式 + UI 打磨

Day 18-19 ── 全员：联调 + Bug 修复
          ── A：回归测试 + 编写部署文档
          ── C：前端最终打磨

Day 20-21 ── 全员：最终验证
          ── A：输出测试报告 + 用HT户手册 + 演示脚本
          ── HT：最终审核 + 演示准备
          ── 全员：整理代码

📌 里程碑：24 个测试用例全部通过，有文档，可演示
```

---

## ⚠️ 开发规范

### 分支管理

```
main                    ← 稳定分支
  ├── feat/auth         ← 组长
  ├── feat/infra        ← B
  ├── feat/frontend     ← C
  └── feat/test         ← A
```

### API 约定

- 统一 `/api/v1/` 前缀
- 统一响应：`{ code, message, data }`
- 错误码：200 成功，400 参数错误，401 未登录，403 无权限，500 服务器错误

### 沟通约定

- 接口变更提前通知
- 遇到阻塞立即上报

---

## 🎯 最终交付物

| 交付物 | 负责人 | 说明 |
|--------|--------|------|
| 可运行的系统 | 全员 | Docker 一键启动 |
| 认证 + 权限 | 组长 | JWT + Spring Security |
| Redis + 持久化 | B | 任务/提交数据持久化 |
| 学生端页面 | C | 登录/注册/仪表盘/历史/详情 |
| 24 个测试用例 | A | 全部通过 + 测试报告 |
| API 文档 | A | `docs/API.md` |
| 部署文档 | A | `docs/DEPLOY.md` |
| 用户手册 | A | `docs/USER-GUIDE.md` |

---

*本文档由技术负责人制定，如有疑问请讨论后调整。*
*最后更新：2026-05-21*
